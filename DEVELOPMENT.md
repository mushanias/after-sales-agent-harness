# 售后处理 Agent 开发文档

这份文档与代码共同演进，只记录当前已经确认或正在实现的内容。

## 项目定位

这是一个用于实习展示的轻量售后 Agent Harness 原型。项目重点是说明模型如何通过 Harness 获取观察并采取行动，同时展示清晰契约、测试、错误边界和可追踪的 Git 演进。

核心原则：

```text
Agent = Model + Harness
```

模型负责判断是否调用工具以及何时结束；Harness 负责把工具暴露给模型、执行调用，并把真实结果送回模型。

## 当前阶段：最小 Agent Loop

当前使用者在命令行输入售后问题。一次完整流程是：

```text
用户输入
→ 模型响应
→ 有 tool_use：Harness 查询模拟订单并回传结果，再次调用模型
→ 无 tool_use：输出模型最终回答，本轮结束
```

当前仅有一个只读工具 `lookup_order`，数据来自内存中的模拟订单。消息只保留在当前进程内，不做持久化。

### 当前不变量

1. 模型没有发出工具调用时，不追加空的工具结果消息。
2. 工具结果携带原工具调用 ID，保证模型能对应请求和结果。
3. Agent 只能依据工具结果回答订单信息，系统提示明确禁止编造。
4. 本阶段工具只读，不产生退款、建单等外部副作用。

### 当前明确不做

- 多工具注册和统一分发
- 退款等写操作与人工审批
- 会话或工单持久化
- Hooks、上下文压缩、记忆和 Goal Loop
- 多 Agent、定时任务和 MCP
- 真实电商平台接入

## 技术选择

- Python 命令行程序
- Anthropic Messages API，保持与参考学习项目的最小循环一致
- 标准库 `unittest`，让循环测试不依赖真实模型或付费 API
- 工具契约暂用 JSON Schema，并在执行前校验唯一的字符串参数；工具输入变复杂时再引入 Pydantic

## 本地运行

安装依赖：

```powershell
pip install -r requirements.txt
```

设置环境变量并启动：

```powershell
$env:ANTHROPIC_API_KEY = "你的密钥"
$env:MODEL_ID = "你要使用的模型 ID"
python main.py
```

运行不访问真实模型的测试：

```powershell
python -m unittest -v
```

## 下一步候选

完成并验证当前循环后，再由项目负责人选择下一项。当前最自然的候选是增加第二个售后工具，并把工具执行改为注册表分发。
