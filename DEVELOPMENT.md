## day 1
## 1. 最小 Loop

`agent_loop` 只保留模型调用、消息追加、停止判断和工具结果回填。售后工具的具体实现不进入循环，工具调用的解析也交给 `execute_tool`。

当前没有实现观测性。以后需要记录模型耗时、工具调用、异常或 Token 使用时，可以在模型调用前后和工具执行前后挂 Hook，而不需要改变循环的判断结构。

## 2. 工具注册与分发

工具有两个入口：`TOOLS` 是提供给模型的函数声明，`TOOL_HANDLERS` 是运行时使用的函数表。两者通过工具名对应。

`execute_tool` 是模型工具调用与普通 Python 函数之间的边界：它解析模型给出的参数，根据工具名找到函数，调用函数，并把结果重新序列化后交还给模型。当前订单工具的声明已经存在，handler 尚未注册。

## 3. 模型配置池

模型侧通过 `ModelConfig` 统一提供模型 ID、API 地址和密钥环境变量名，`MODEL_POOL` 预留了按名称选择模型的入口。

它目前还不是实际的连接池：项目只有一个 DeepSeek V4 Pro 配置，客户端仍在 `main` 中直接创建，模型配置也都是硬编码。现阶段只使用这个形状隔离模型配置，不提前实现连接复用、动态加载或故障切换。

## 4. Main 与当前失败边界

`main` 只负责选择模型、创建客户端、维护当前进程内的消息列表，以及把用户输入交给 loop。

当前 loop 没有模型调用异常、超时、重试和循环次数限制。工具侧只处理参数解析、未知工具以及函数参数或业务值异常。这个失败边界是当前阶段有意保留的，暂不补齐。

## 5. hook
我们统一在init中通过工具函数注册

## day 2
今天跑通了闭环


## 1
我们把loop摘了个干净，全部通过hook和中间件统一管理，hook使用trigger_hooks（）调用
边界很清楚不做赘述

## 2
后面我们设计prompt的编排，目前通过字段和硬编码传输知识，我们的契约不应该对loop生效
而是直接用工具对接loop
